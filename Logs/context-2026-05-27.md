# Context — 2026-05-27 (Engineer session)

## What this session was about

Infrastructure documentation, backup fix, and OpenClaw job registry. No changes to Guide's behaviour — purely housekeeping and observability.

Three goals:
1. Ensure every `/srv` directory has a CLAUDE.md so future sessions can orient quickly without needing to re-explore the machine.
2. Fix the OpenClaw backup script, which has been failing silently every night since the Mac Mini → Z8 migration in May.
3. Document all scheduled automation in a single CRON.md and make it self-maintaining.

---

## Decisions made

### CLAUDE.md coverage strategy

Wrote a top-level `/srv/CLAUDE.md` as the entry point (directory map, services table, critical rules). Then a per-directory CLAUDE.md for each subdirectory. Deferred heavily to existing docs (guide-core/CLAUDE.md, guide-build/CLAUDE.md) rather than duplicating. Placeholder directories (hermes, landing-pages, logs, onedrive, openwebui) got short files that explain their status and what activating them would require.

Also created `/srv/CRON.md` at the suggestion of the session. Rationale: all scheduled automation is in a single crontab and deserves one readable reference with context (status, blockers, env vars). Per-directory CRON.md files were considered and rejected — they'd scatter information that's best seen together.

### Backup script — run as root via sudo

The backup script needed to read `600`-mode files owned by the `guide` user (auth tokens, agent state). Options considered:
- `tar --ignore-failed-read` — skips unreadable files gracefully, but those files are worth having in the backup
- Root's crontab — would work, but gareth manages the crontab, not root
- NOPASSWD sudo rule for the specific script — chosen. Clean, auditable, least-privilege for this one script.

Added `/etc/sudoers.d/openclaw-backup` and updated crontab entry to `sudo /srv/guide-core/scripts/openclaw-backup.sh`.

### Archive size — what to exclude

Discovered the naive archive was 731MB. Traced large contributors:
- `agents/*/sessions/` — 750MB+ of conversation trajectory `.jsonl` files. Not needed for recovery (agent identity and memory are in `workspaces/`). Excluded.
- `_inbox/` — 670MB staging dir + Mac Mini migration artefacts. Excluded.
- `npm/` — 509MB npm packages. Reinstallable. Excluded.

Result: 37MB archive containing config, workspaces, credentials, skills, and agent auth state.

Note: `memory/` was excluded in the original Mac Mini script but the rewrite dropped that exclusion — agent memory **is** included in the archive. Verified: `openclaw/memory/*.sqlite` present in `openclaw-20260527-1122.tar.gz`.

### CRON.md — self-maintaining via OpenClaw

Created `/srv/guide-core/CRON.md` (symlinked from `/srv/CRON.md`) documenting both host crontab jobs and all 7 OpenClaw native jobs. The OpenClaw section is wrapped in `<!-- BEGIN/END OPENCLAW CRON -->` markers and auto-regenerated by `sync-cron-md.py` + `sync-cron-md.sh`.

Key discovery: **Claude Code runs on the HOST as gareth, not inside the container.** This matters because:
- `cron/jobs.json` is `700 guide` — readable inside container, not by gareth on host
- `git push` needs SSH keys — available on host as gareth, not inside container
- Solution: `sync-cron-md.py` tries direct file open first (works inside container), falls back to `docker exec openclaw cat` on `PermissionError` (works from host). Guide diagnosed and fixed this during the test run.

OpenClaw cron job `b8a528aa` runs the sync every Sunday 03:30 UTC. First run: 2026-06-01.

---

## Key findings

- **Backup was broken for ~8 days** (since Z8 migration 2026-05-19). The crontab ran nightly but all runs failed with `tar: .openclaw: Cannot stat: No such file or directory`. The log at `/srv/backup/dumps/backup.log` shows 6 failure entries (the last 6 nightly runs before today).
- **OpenClaw native cron is live.** 7 jobs in `/srv/openclaw/cron/jobs.json`. The `openclaw.json` `cron` section is only runtime config (concurrency, retry). CHUNK-08 is effectively complete.
- **Two cron jobs are paused** since 2026-04-26: pulse reports and LLM reports. Both blocked on OneDrive not being mounted at `/srv/onedrive/`. Log paths they write to don't exist yet.
- **`compose` and `ollama` git repos have no GitHub remote.** Commits are local-only. This is pre-existing state.
- **`guide-staging` and `guide-vaults` are not git repos.** CLAUDE.md files created there exist only on the filesystem.

---

## What was NOT done (and why)

- **memory/ in backup** — confirmed included (the rewrite dropped the original exclusion). Verified in archive.
- **OneDrive setup** — out of scope; `/srv/onedrive/CLAUDE.md` documents how to do it when ready.
- **Adding remotes to compose/ollama repos** — will happen this week per Gareth.
- **Clearing backup.log** — left to self-heal as successful runs append to it.

---

## Immediate next actions

1. **Mount OneDrive** at `/srv/onedrive/` (rclone — see that CLAUDE.md) to unblock pulse report and LLM report cron jobs.
2. **Continue Phase 1 build** — CHUNK-11 Paperclip, CHUNK-15 Keith instance, CHUNK-16 Hadley instance.
3. **Add GitHub remotes** to `compose` and `ollama` repos (Gareth: this week).
