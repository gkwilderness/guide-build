---
title: "context-2026-05-26"
type: log
area: infra
project: Guide
tags: [guide, agents, personal-instance, matt, telegram, sales-brief, session-log]
date: 2026-05-26
---

# Session Context — 2026-05-26

Engineer session. Focus: build Matt Wylie's personal Guide agent from scratch and wire the sales-brief skill.

---

## Investigation

### Did we have Matt's Telegram details?

Initial assumption: yes. Reality: we had Matt's **chat ID** (`8265788167`) from CHUNK-06 (he was wired as an `allowFrom` operator on the main bot), but had no personal bot token. A personal instance needs its own bot. Gareth created `@WildernessMattBot` via BotFather and provided the token during the session.

### How is sales-brief wired?

Investigated Nick and Hadley's workspaces. The skill is wired via two manual file edits post-generation:
- `BOOT.md` — adds a "Sales Pipeline Questions" section with the load instruction and rules
- `TOOLS.md` — adds two `vault_read` lines: one for `/srv/guide-outputs/Wilderness/hubspot/` and one for `/srv/openclaw/skills/sales-brief/`

The generate.sh templates do not include the sales-brief wiring — it must be added per-agent after generation. This is the established pattern.

### Vault access decision

User specified: sales and hubspot vaults. Investigation found:
- No `hubspot` team vault exists on disk — the HubSpot data is at `/srv/guide-outputs/Wilderness/hubspot/` (covered by the skill) and within the `digital` team vault
- Matt doesn't need the digital vault — he just needs the pipeline reports
- Conclusion: sales-brief skill alone covers his data need. No team vault mount required.

Boot context (PRIORITIES.md, FY27-CEO-Commitments.md) lives in the exco vault. Since Matt has no exco access, "same boot context as Hadley" was not possible. Gareth confirmed: skip boot context entirely for now.

### API key

Three keys exist: `exec` (Nick-approved, Claude Max Plus), `domain` (not yet provisioned), `operational` (current live key). Matt assigned `operational` — same as channel agents. Upgrade when usage warrants it.

---

## Decisions

| Decision | Reasoning |
|---|---|
| No team vaults | Matt is a read-only consumer; sales-brief skill covers his data need; no write use case identified |
| No boot context | Exco vault off-limits; Matt's priorities unknown — to be collected from him later |
| Operational API key | Domain key not provisioned; exec key reserved for exec tier; operational is the pragmatic default |
| Hybrid review mode | Consistent with Nick; Matt is finance, judgment calls should escalate |
| Telegram only for personal instance | Consistent with all other personal instances; Slack DMs go to main agent (existing behaviour, no change) |

---

## What Was Built

```
/srv/openclaw/workspaces/personal-matt/   — workspace (generated by factory)
  BOOT.md     — sales-brief wiring added manually post-generation (440)
  TOOLS.md    — two vault_read lines added (664)
  SOUL.md, IDENTITY.md, USER.md, AGENTS.md, BOOTSTRAP.md — factory-generated (440)
  MEMORY.md, HEARTBEAT.md — agent-writable (664)
  ONBOARDING.md — factory-generated (440)

/srv/guide-vaults/personal/matt/          — personal vault directory (empty)
/srv/guide-core/__CONFIG/keys/telegram-matt  — bot token (640, gitignored)
```

openclaw.json changes (3):
1. `channels.telegram.accounts.matt` — bot config, allowFrom [8265788167, 6864752167]
2. `agents.list` — personal-matt agent entry
3. `bindings` — route personal-matt ← telegram accountId:matt

roster.json: `persons.matt` added. Gates: personSpecWritten, apiKeyProvisioned, templateReady, telegramBotCreated, telegramChatIdCollected, workspaceGenerated, registeredInOpenClaw all `true`.

---

## Known Issues / Watch Points

- `personal-nick/ONBOARDING.md` missing — Nick's agent tries to load it and fails. Pre-existing issue, not introduced this session.
- `sqlite-vec` unavailable in container — vector recall degraded for all agents. Pre-existing.
- Matt's agent tried to load `PRIORITIES.md` from personal vault on first message — failed silently (expected). The SOUL.md "load context from mounted vault paths" instruction causes this; it's benign.

---

## Immediate Next Actions

1. Gareth to test Matt's bot directly before handing the link to Matt
2. Collect Hadley's chat ID when available → update `openclaw.json allowFrom` + `roster.json telegramChatId`
3. When Matt has context to share: write `/srv/guide-vaults/personal/matt/PRIORITIES.md`
